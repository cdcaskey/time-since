# Default config, seeded into $CONFIG_DIR on first container start if no
# .env already exists there. Safe to edit in place on the host — the
# entrypoint never overwrites a file that's already present.
#
# Real environment variables (e.g. set via docker-compose `environment:`)
# always take precedence over this file.

PORT=3000
LOG_LEVEL=info
